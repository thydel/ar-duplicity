---

secret:
  key: >-
    {{ paths.etc }}/{{ inventory_hostname }}
  ssh:
    name: >-
      {{ inventory_hostname }}-{{ name }}
    key: >-
      {{ paths.etc }}/{{ inventory_hostname }}-{{ name }}
    fetched_pub_key: >-
      {{ paths.lkeys }}/{{ inventory_hostname }}-{{ name }}.pub
